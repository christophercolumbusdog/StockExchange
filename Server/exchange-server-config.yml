# Database settings.
database:
  # the name of your JDBC driver
  driverClass: org.hsqldb.jdbc.JDBCDriver
  # the username
  user: sa
  # the password
  password: sa
  # the JDBC URL
  url: jdbc:hsqldb:target/exchange
  validationQuery: SELECT * FROM INFORMATION_SCHEMA.SYSTEM_TABLES

#server:
#  type: default
#  maxThreads: 4
#  minThreads: 2
#  maxQueuedRequests: 2048
#  applicationConnectors:
#      - type: http
#        acceptorThreads: 1
#        selectorThreads: 1